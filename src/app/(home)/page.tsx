import Blogs from '@/components/Blogs';
import Hero from '@/components/Hero';

const HomePage = () => {
  return (
    <main>
      <Hero />
      <Blogs />
    </main>
  );
};

export default HomePage;
